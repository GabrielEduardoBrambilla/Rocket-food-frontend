import { Container } from './styles';

// eslint-disable-next-line react/prop-types
export function Textarea({ ...rest }) {
  return (
    <Container {...rest}>
      {/* {value} */}
    </Container>
  )
}